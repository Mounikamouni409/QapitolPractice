package com.qapitol.pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

import com.qapitol.utilities.Utilities;


public class Elements extends Utilities {
	
	WebDriver driver;
	
	public Elements(WebDriver driver) {
		this.driver = driver;
	}
	
	
	By elements = By.xpath("//h5[text()='Elements']/ancestor::div[@class='card mt-4 top-card']");
	//By text = By.xpath("//span[normalize-space()='Text Box']/parent::li");
	By text = By.xpath("//li[@id='item-0']/child::span[normalize-space()='Text Box']");
	
	public void elements() {
		
		waitForElementToBeClickable(driver, 10, elements);
		WebElement element = driver.findElement(elements);
		javascriptExecutor(driver, element);
		clickMethod(element);
		
	}
	
	public void textBox()
	{
		WebElement textbox = driver.findElement(text);
		javascriptExecutor(driver, textbox);
		staleElement(driver,textbox);
		//clickMethod(textbox);
	}
}
